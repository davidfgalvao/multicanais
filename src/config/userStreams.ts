/**
 * Suas URLs oficiais por ID do jogo (veja `id` em `src/data/matches.ts`).
 *
 * Para produção na Vercel sem commitar URLs: use
 * NEXT_PUBLIC_STREAMS_JSON nas variáveis de ambiente do projeto (veja `.env.example`).
 */
export const USER_STREAM_URLS: Record<string, string> = {
  // Só para validar o fluxo; troque pelas suas URLs oficiais.
  "1": "https://example.com",
};
